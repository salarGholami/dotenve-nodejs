const { default: axios } = require("axios");

require("dotenv").config();

console.log(process.env.SECRET_KEY);

// console.log(`connecting to port 5000`);

// axios.get("https://api.koft.ir");

//1.clean code
//2. security
